# ══ OpenClaw Orchard — Environment Variables ══
# Copy this file to .env and fill in all values
# NEVER commit .env to version control
# NEVER paste these values into any agent prompt or Company Wiki

# ── Gemini (Primary) ──
GEMINI_API_KEY=

# ── OpenRouter (Fallback 1) ──
OPENROUTER_API_KEY=

# ── DeepSeek (Fallback 2) ──
DEEPSEEK_API_KEY=

# ── Perplexity (Web Search) ──
PERPLEXITY_API_KEY=

# ── Helius RPC (Solana on-chain) ──
HELIUS_API_KEY=

# ── Supabase ──
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=

# ── Telegram Bots ──
# Create three separate bots via @BotFather
TELEGRAM_MANAGER_BOT_TOKEN=
TELEGRAM_AUDITOR_BOT_TOKEN=
TELEGRAM_FINANCE_BOT_TOKEN=
TELEGRAM_CEO_CHAT_ID=   # Your personal Telegram chat ID

# ── AK.pro (manual use only — no API yet) ──
# AK_PRO_API_KEY=       # Uncomment when API integration is ready

# ── Sports Data (Prediction God) ──
THE_ODDS_API_KEY=

# ── Optional: Apify (TikTok scraping for Factory) ──
APIFY_API_KEY=
